# API CORS origins, separated by commas.
MEDINSIGHT_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# PostgreSQL settings used by docker compose.
POSTGRES_DB=medinsight
POSTGRES_USER=medinsight
POSTGRES_PASSWORD=change-me-for-shared-environments